# spec: least-action-lab (최소작용 원리 실험실)

## 1. 선택한 카테고리와 웹앱

- **카테고리**: `해석역학과_장이론` (`data-category`에 그대로 사용)
- **앱 이름(폴더명)**: `least-action-lab`
- **한 줄 설명**: 시작점과 끝점이 고정된 경로들 중, 자연이 실제로 택하는 경로는 왜 "작용(action) S"를 극값으로 만드는 바로 그 경로인지를 직접 경로를 조작해보며 확인하는 인터랙티브 실험실.

### 원본 근거

`/mnt/c/Users/hugok/Claude/Projects/doc/해석역학과_장이론/해석역학_소스.tex` 1장(해석역학의 탄생) 후반부
"또 다른 길: 최소작용 원리" ~ "해밀턴의 원리" 절(694~756행)을 직접 확인했다. 핵심 내용:

- 작용 `S[q(t)] = ∫_{t0}^{t1} L(q, q̇, t) dt` (L = T − V)는 경로 전체를 입력받아 숫자 하나를 내놓는 범함수다.
- 해밀턴의 원리: 실제 물리적 경로는 이웃한 모든 경로들 가운데 S를 정류(주로 최소)값으로 만드는 경로다.
- 이 조건(δS = 0, 양끝점 고정)에서 부분적분과 "임의의 δq에 대해 항상 0이려면 계수가 0"이라는 논리로 오일러-라그랑주 방정식이 유도됨을 보였다.
- 책은 이 관점이 이후 7장(페르마의 원리), 12장(슈뢰딩거 방정식), 14장(파인만 경로적분), 25장(측지선 방정식)까지 반복해서 등장하는 "물리학 전체를 관통하는 문법"이라고 명시한다.

이 개념은 텍스트로는 이해하기 어렵지만, 사용자가 직접 후보 경로를 만들어보고 그 작용값을 눈으로 비교하면 "왜 하필 포물선/직선/사인곡선이 답인가"가 즉시 체감된다는 점에서, 단순 역사 서술(OS 후보)이나 학부 회로 시뮬레이터(반도체 후보)보다 훨씬 더 "조작하며 직관을 얻는" 인터랙티브 웹앱에 적합하다고 판단했다.

## 2. 핵심 기능 목록

1. **시나리오 선택** — 라그랑지안 L = T − V가 서로 다른 3가지 상황을 드롭다운으로 전환한다.
   - 자유입자(중력·힘 없음, L = ½mv², 정답 경로 = 등속 직선)
   - 균일 중력장 속 포물체(L = ½mv² − mgy, 정답 경로 = 포물선)
   - 조화진동자/용수철(L = ½mv² − ½kx², 정답 경로 = 사인곡선)
2. **경로 편집(드래그)** — 시작점 (t0,q0)·끝점 (t1,q1)은 고정, 그 사이 3~5개의 제어점을 캔버스에서 마우스/터치로 드래그해 자유로운 후보 경로 q(t)를 만든다(제어점 사이는 스플라인/선형 보간).
3. **실시간 작용값(S) 표시** — 현재 경로를 시간 구간으로 나눠 L을 수치적분(사다리꼴/심프슨)해 S를 계산하고, 화면에 실시간으로 갱신 표시한다. "이 경로가 물리적으로 가능한가"를 직관적으로 보여주기 위해 뉴턴 법칙(F=ma) 위반 여부와 무관하게 아무 경로나 시험해볼 수 있게 한다.
4. **정답 경로 오버레이·비교** — "정답 보기" 토글로 오일러-라그랑주 방정식의 해석해(직선/포물선/사인곡선)를 캔버스에 겹쳐 그리고, 그 경로의 S값과 현재 사용자 경로의 S값을 나란히 비교(막대 또는 숫자 diff)한다.
5. **작용 지형(action landscape) 그래프** — 정답 경로에 사인 모드 섭동 `q(t) + a·sin(nπ(t−t0)/(t1−t0))`을 더한 뒤, 섭동 진폭 a를 슬라이더로 스윕하며 S(a)를 계산해 꺾은선 그래프로 그린다. a=0(정답 경로)에서 S(a)가 이차함수 형태의 최솟값(정류점)을 갖는 것을 시각적으로 확인시킨다.

## 3. 파일 구조

```
/apps/least-action-lab/
├── index.html
├── css/
│   └── style.css
└── js/
    ├── main.js        # UI 초기화, 시나리오 전환, 제어점 드래그 이벤트, 화면 갱신 루프
    ├── physics.js      # 시나리오별 라그랑지안 L(q,q̇,t), 해석해(정답 경로), 작용 S 수치적분
    ├── path-editor.js  # 캔버스 위 경로 그리기, 제어점 드래그·보간(스플라인/선형)
    ├── landscape.js    # 섭동 진폭 스윕 → S(a) 계산 및 Chart.js 연동
    └── vendor/
        └── chart.umd.min.js   # 기존 apps/kepler-orbit-sim/js/vendor/ 등에서 복사해 재사용
```

## 4. 주요 UI 구성

- **상단**: 시나리오 선택 드롭다운(자유입자 / 포물체 / 조화진동자), 간단한 설명 텍스트(해당 시나리오의 L 수식).
- **좌측(또는 상단) 메인 캔버스**: 가로축 = 시간 t, 세로축 = 위치 q. 시작점·끝점(고정, 색 다르게 표시), 사용자가 드래그하는 제어점과 그 보간 곡선(파란색 등), "정답 보기" 토글 시 해석해 곡선(주황색 점선 등)을 겹쳐 표시.
- **정보 패널**: 현재 경로의 작용값 S(사용자 경로) 숫자, 정답 경로의 작용값 S(정답), 두 값의 차이(ΔS ≥ 0임을 강조 — 즉 정답이 항상 최소).
- **하단 보조 그래프(Chart.js line chart)**: 작용 지형 S(a) vs 섭동 진폭 a. 슬라이더로 a를 움직이면 그래프 위 현재 위치에 점(marker)이 실시간으로 표시되고, 동시에 메인 캔버스의 경로도 그 섭동만큼 흔들린 모습으로 갱신된다.
- **리셋 버튼**: 제어점을 정답 경로 위치로 되돌림.
- 모바일 대응: 캔버스는 터치 드래그 지원, 좁은 화면에서는 메인 캔버스 → 정보 패널 → 보조 그래프 순으로 세로 스택.

## 5. 사용할 기술

- 순수 HTML/CSS/JS. 프레임워크 없음.
- 캔버스 2D API로 경로 편집기(메인 그래프) 직접 구현(드래그 가능한 제어점 UI는 라이브러리 없이 구현 가능한 난이도).
- 보조 그래프(작용 지형 S(a))는 기존 관례를 따라 **Chart.js**를 `js/vendor/chart.umd.min.js`로 로컬 복사해 사용한다. 이미 `apps/kepler-orbit-sim/js/vendor/`, `apps/link-budget-calc/js/vendor/` 등 6개 앱에 동일 파일(`chart.umd.min.js`)이 존재하므로, Build 단계에서 그대로 복사해 재사용 가능한지 확인할 것(버전 동일하면 복사, CDN 새로 받지 않음).
- 수치적분(사다리꼴 또는 심프슨 공식)은 별도 라이브러리 없이 `physics.js`에서 직접 구현.

## 6. 구현 범위 외

- 2차원/3차원 경로(예: 실제 포물체의 x-y 평면 운동, 케플러 궤도 등)는 다루지 않는다. 이번 버전은 1차원 좌표 q(t) 대 시간 t 평면으로 한정한다.
- 페르마의 원리(광학), 슈뢰딩거 방정식·경로적분, 측지선 방정식(일반상대성) 등 같은 장 뒷부분/이후 장에서 "최소작용 원리가 재등장하는" 다른 맥락은 이번 앱에서 다루지 않는다(추후 별도 앱 후보로 남겨둠).
- 마찰력 등 비보존력이 있는 경우(경로에 의존하는 일)는 다루지 않는다. 3가지 시나리오 모두 보존력계로 한정.
- 오일러-라그랑주 방정식을 사용자가 직접 유도해보는 수식 입력 기능은 없음(해석해는 앱에 미리 내장).
- 임의의 자유형(free-form) 곡선 드로잉은 지원하지 않는다. 제어점 개수(3~5개)로 제한된 보간 곡선만 지원.
