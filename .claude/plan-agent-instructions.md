# Plan 서브에이전트 지침

## 목표
doc 프로젝트의 PDF 교재 중 하나를 골라 인터랙티브 웹앱 하나를 기획하고 spec.md를 작성한다.

## 중요: 카테고리 선택 제약

기존 웹앱 3개(doppler-sim, constellation-sim, link-budget-calc)가 모두 "위성통신" 카테고리다.
포트폴리오 다양성을 위해 **이번에는 위성통신을 선택하지 않는다.** 아래 5개 후보 중에서 고른다.

## 후보 1: 머신러닝과_인공지능 (원본: 머신러닝과_인공지능_소스.tex)
베이즈학습(MLE/MAP) → 선형/로지스틱 회귀(경사하강법) → 군집화(k-평균/GMM/EM) → SVM(라그랑주/KKT/커널트릭) →
신경망(퍼셉트론/역전파) → CNN → RNN/LSTM → 어텐션/트랜스포머 → LLM(스케일링 법칙/RLHF/LoRA)까지 다룸.

## 후보 2: OS (원본: OS_소스.tex)
운영체제 역사·계보. 배치처리→멀티프로그래밍→유닉스 계보(BSD/GNU/Linux)→개인용 OS 전쟁(DOS/Mac/Windows)→
모바일(iOS/Android)→오늘날 시장 지형(리눅스 서버/클라우드 점유율, HarmonyOS 등). 수식보다 역사/계보 중심.

## 후보 3: 해석역학과_장이론 (원본: 해석역학_소스.tex)
달랑베르 원리→오일러-라그랑주 방정식→최소작용원리→해밀턴 정준형식→푸아송 괄호→슈뢰딩거 방정식→노터의 정리→
맥스웰 방정식→특수/일반상대성이론(측지선, 아인슈타인 방정식, 슈바르츠실트 해)→양자장론(파인만 다이어그램).

## 후보 4: 물리전자와_반도체공학 (원본: 없음, 해당 폴더 CLAUDE.md 참고)
드루드 모형→좀머펠트 자유전자모형→블로흐 정리→크로니히-페니 모형(밴드갭)→캐리어 통계(페르미-디랙)→
드리프트/확산전류→PN접합(쇼클리 다이오드 방정식)→BJT→MOSFET(문턱전압/전류방정식).

## 후보 5: 무한과_극한 (원본: 없음, 해당 폴더 CLAUDE.md 참고)
유리수의 균열→데데킨트 절단으로 실수 구성→완비성 공리→ε-N/ε-δ 논법→단조수렴정리→코시 판정법→
급수 수렴판정→칸토어 대각선논법(비가산성)→칸토어의 정리→연속체 가설.

## 작업 절차

1. `/mnt/c/Users/hugok/Claude/Projects/doc/{후보폴더}/` 안의 최종 tex/pdf 소스를 실제로 읽어 확인한다(위 요약은 참고용 개요일 뿐, 세부 수식/개념은 원본에서 직접 확인할 것).
2. 5개 후보 중 **가장 교육적이고 인터랙티브한 웹앱을 만들 수 있는** 하나를 선택한다(단순 텍스트 요약이 아니라, 사용자가 파라미터를 조작하며 직관을 얻을 수 있는 시각화/시뮬레이터 형태를 우선).
3. 선택한 폴더 안에서 구체적으로 어떤 개념을 웹앱으로 만들지 정한다(예: MLP 학습 시각화, 유닉스 계보 트리, 라그랑주 역학 진자 시뮬레이터, PN접합 밴드다이어그램, 데데킨트 절단 시각화 등 — 예시일 뿐 원본 확인 후 직접 판단).

## 출력 요구사항

`/home/hugok/MY-BLOG/spec.md` 파일을 다음 항목으로 작성한다:

1. **선택한 카테고리와 웹앱** — 카테고리명(위 5개 중 하나, `data-category`에 그대로 쓸 것), 앱 이름, 한 줄 설명
2. **핵심 기능 목록** (5개 이내)
3. **파일 구조** — /apps/{앱이름}/ 하위 파일 목록
4. **주요 UI 구성** — 어떤 입력/출력/그래프가 있는지
5. **사용할 기술** — 순수 HTML/CSS/JS, 벤더링할 라이브러리(예: Chart.js — 기존 관례상 CDN보다 `js/vendor/`에 로컬 복사 선호, 필요시 `apps/link-budget-calc/js/vendor/` 등 기존 사본 재사용 가능한지 확인)
6. **구현 범위 외** — 이번 버전에서 제외할 기능

spec.md 작성 후 완료를 보고한다. 구현은 하지 않는다.
